# Consistency record for a pair of documents. Both files listed below must say the
# same thing; these are the git blob hashes of each side at the last confirmed-
# consistent state, so editing one side without re-recording goes red in
# `test/docs.test.ts`.
#
# After editing either side, bring the other along and re-record the pair:
#
#   git hash-object README.md README.en.md
#
# That test recomputes these hashes with `node:crypto`, so no git and no repository
# are needed, and it also checks that the two sides keep the same structure.
README.md: 943810cd0abf98ee77fed5cd60e1d6809bf7ebf1
README.en.md: 53e24d50408a72c49c8e702ca866cd995b71022f
